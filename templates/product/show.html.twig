{% extends "base.html.twig" %}

{% block title %}
	Page de
	{{ product.name }}
{% endblock %}

{% block body %}
	<ol class="breadcrumb">
		<li class="breadcrumb-item">
			<a href="{{ path('home') }}">Accueil</a>
		</li>
		<li class="breadcrumb-item">
			<a href="{{ path('product_category', {'id': product.category.id}) }}">{{ product.category.name }}</a>
		</li>
		<li class="breadcrumb-item active">{{ product.name }}</li>
	</ol>

	<h1>{{ product.name }}
		({{ product.price | amount }})</h1>

	<div class="row">
		<div class="col-4">
			<img class="img-fluid" src="{{ product.image }}" alt="Image de {{ product.name }}">
		</div>
		<div class="col">
			<span class="badge badge-primary">
				{{ product.category.name }}
			</span>
			<p>{{ product.description }}</p>
			<a href="#" class="btn btn-success btn-lg">
				<i class="fas fa-shopping-cart"></i>
				Ajouter au panier
			</a>
		</div>
	</div>
{% endblock %}
